/**
 * ====== ملف إعدادات قاعدة البيانات ======
 * 
 * هذا الملف يحتوي على إعدادات الاتصال بقاعدة البيانات
 * يمكنك تعديل القيم حسب إعدادات XAMPP الخاصة بك
 */

// رابط API الأساسي (اتركه فارغاً إذا كان المشروع في نفس المجلد)
window.API_BASE_URL = '';

// معرف المستأجر (اختياري - للاستخدام في أنظمة متعددة المستأجرين)
window.TENANT = null;

// إعدادات إضافية
window.DEBUG_MODE = false; // تفعيل وضع التصحيح

console.log('✅ تم تحميل إعدادات قاعدة البيانات');

